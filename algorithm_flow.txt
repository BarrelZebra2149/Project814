1. 유전 알고리즘 활용. 무지성으로 uniformInt()를 활용한 돌연변이(일정 확률로 아무 숫자나 바꿔주세요)
2. 2000점대 돌파. 유전 알고리즘은 자식을 낳을떄 매겨지는 점수인 fitness라는 것이 있는데, 원래 값에 가중치를 붙여주는 휴리스틱 부여. 목표로 하는 값이 읽히는 횟수가 1에 가깝도록 코드를 짜도록 했음. 이러한 경험적인 값을 fitness라고 함.
3. 0~9 에 대한 fitness값을 1로 해주었더니 2000점대에서 2307점으로 올라감.
4. 휴리스틱에 대한 MAX값이 너무 안 올라가서 확률 조정.
5. 또 안됨. 우선순위를 매겨서 점수가 높은 것이 더 올라가도록 값을 조정함.
6. 안됨. 값이 바뀌어지는 이벤트인 돌연변이가 일어날 확률을 변경.
7. 소용없었음. 1000~9999까지 만들 수 있는 계수를 확인하는 것도 우선순위로 두기로 함.
8. 소용없었음. 숫자 안에있는 특정 숫자를 다른 숫자로 서로 바꿔치기 하는 방식을 활용하도록 함.
9. 오히려 점수가 2277점으로 떨어졌음. 다양성을 우선으로 최고의 자식을 찾아주는 tools.selTournamentDCD 라는 것을 알게 됨. 이를 위해서는 서로같의 비슷함을 측정해주는 crawding 값이 필요했음.
10. 선형으로 교배를 했던 것을 2차원식 교배로 변경.
11. 유의미하게 올라가나 다양성이 부족. crowding_distance 도입.
12. 4000점 달성
13. 더이상 올라가지 않음. 아래 코드 돌입.
if STAGNATION_MODE:
        r = random.random()
        if r < 0.40: candidates = tools.selBest(pop, select_size)
        elif r < 0.50: candidates = tools.selRandom(pop, select_size)
        elif r < 0.65: candidates = tools.selNSGA2(pop, select_size, nd=nd_select)
        else: candidates = tools.selTournamentDCD(pop, select_size)
    else:
        candidates = tools.selTournamentDCD(pop, select_size)
14. 방향에 따라 값 생성 방식을 다르게 하는 코드 생성
15. missing hunter << 너무 에바임. 삭제
16. 3x3 rotate, shift mutation 코드 생성
17. stagnation시 SA 사용
18. 돌연변이 확률 대폭 상승, 유전 확률 줄이기.